# Q2. Bit Operations - 3

## Problem Description

What happens to the **0-th bit** in `a` when we perform:

```cpp
a = a ^ 1
```

### Options

- A. It gets toggled
- B. It gets unset
- C. It gets set
- D. It remains same